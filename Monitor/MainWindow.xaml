<Window x:Class="Monitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Monitor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width= "*" />
            <ColumnDefinition Width= "100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1" Background="LightGray">
            <Button  Click="Button_Click" Name="PlayButton" >Play</Button>
            <Button Click="CancelButton_OnClick"  Name="CancelButton" >Cancel</Button>
            <ComboBox Name="ScoreCombo" StaysOpenOnEdit="True">
            </ComboBox>
        </StackPanel>
        <DockPanel Grid.Column="0" Name="MainPanel" SizeChanged="MainPanel_OnSizeChanged">
            <Canvas Name="MainCanvas" Background="White"></Canvas>
        </DockPanel>
    </Grid>
</Window>
